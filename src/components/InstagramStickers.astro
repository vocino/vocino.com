---
---

<section class="home-stickers" id="home-stickers" hidden aria-label="Logo sticker photos from Instagram">
  <h2 class="home-stickers__heading heading-bracket">In the wild</h2>
  <p class="home-stickers__lede">
    <span class="label-telemetry">field_feed</span>
    Sticker slaps on Instagram — tagged <span class="home-stickers__tag">#vocino</span>.
  </p>
  <ul class="home-stickers__grid" id="instagram-stickers-grid"></ul>
</section>

<script>
  import { formatIsoDate } from '../lib/formatIsoDate';

  async function loadStickers() {
    const section = document.getElementById('home-stickers');
    const grid = document.getElementById('instagram-stickers-grid');
    if (!section || !grid) return;

    try {
      const response = await fetch('/api/instagram-stickers');
      const data = await response.json();

      const stickers = data.stickers || [];
      if (!data.configured || stickers.length === 0) {
        section.remove();
        return;
      }

      grid.replaceChildren();

      for (const s of stickers) {
        const li = document.createElement('li');
        li.className = 'home-stickers__item';

        const link = document.createElement('a');
        link.className = 'home-stickers__card';
        link.href = s.permalink;
        link.target = '_blank';
        link.rel = 'noopener noreferrer';
        link.setAttribute('aria-label', 'View post on Instagram');

        const img = document.createElement('img');
        img.className = 'home-stickers__img';
        img.src = s.imageUrl;
        img.alt = '';
        img.loading = 'lazy';
        img.decoding = 'async';
        img.referrerPolicy = 'no-referrer';
        img.width = 640;
        img.height = 640;

        const meta = document.createElement('div');
        meta.className = 'home-stickers__meta';

        const loc = document.createElement('p');
        loc.className = 'home-stickers__location';
        const dateStr = formatIsoDate(s.timestamp);
        if (s.location) {
          loc.textContent = dateStr ? `${s.location} · ${dateStr}` : s.location;
        } else if (dateStr) {
          loc.textContent = dateStr;
        } else {
          loc.textContent = 'Instagram';
        }

        meta.appendChild(loc);
        link.appendChild(img);
        link.appendChild(meta);
        li.appendChild(link);
        grid.appendChild(li);
      }

      section.hidden = false;
    } catch (e) {
      console.error('Instagram stickers:', e);
      section.remove();
    }
  }

  loadStickers();
</script>

<style>
  .home-stickers {
    width: 100%;
    max-width: min(52rem, 100%);
    margin-top: 5rem;
    padding-top: 4rem;
    border-top: 1px solid var(--border);
    box-shadow: inset 0 1px 0 0 rgba(0, 204, 255, 0.08);
  }

  .home-stickers__heading {
    font-family: 'Source Sans 3', system-ui, sans-serif;
    font-size: 1.125rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--text-primary);
    margin: 0 0 0.35rem;
  }

  .home-stickers__heading.heading-bracket {
    font-family: 'Source Code Pro', ui-monospace, monospace;
    font-size: 0.9375rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-secondary);
  }

  .home-stickers__lede {
    font-family: 'Source Sans 3', system-ui, sans-serif;
    font-size: 0.9375rem;
    line-height: 1.5;
    color: var(--text-secondary);
    margin: 0 0 2.5rem;
    max-width: 36rem;
  }

  .home-stickers__lede .label-telemetry {
    display: block;
    margin-bottom: 0.35rem;
    font-size: 0.75rem;
  }

  .home-stickers__tag {
    color: var(--text-muted);
    font-weight: 500;
  }

  .home-stickers__grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 1.5rem;
  }

  .home-stickers__item {
    margin: 0;
  }

  .home-stickers__card {
    position: relative;
    display: block;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid var(--cyber-frame-dim);
    background: var(--surface-1);
    text-decoration: none;
    color: inherit;
    transition:
      border-color 0.25s ease,
      box-shadow 0.25s ease,
      transform 0.2s ease;
  }

  .home-stickers__card:hover,
  .home-stickers__card:focus-visible {
    border-color: rgba(0, 204, 255, 0.45);
    box-shadow: var(--cyber-glow-soft);
    outline: none;
    transform: translateY(-2px);
  }

  @media (prefers-reduced-motion: reduce) {
    .home-stickers__card:hover,
    .home-stickers__card:focus-visible {
      transform: none;
    }
  }

  .home-stickers__img {
    display: block;
    width: 100%;
    aspect-ratio: 1;
    object-fit: cover;
    vertical-align: middle;
  }

  .home-stickers__meta {
    padding: 1rem 1.25rem;
    border-top: 1px solid rgba(43, 52, 64, 0.95);
    background: linear-gradient(180deg, rgba(22, 27, 34, 0.98), var(--surface-1));
  }

  .home-stickers__location {
    margin: 0;
    font-family: 'Source Code Pro', ui-monospace, monospace;
    font-size: 0.75rem;
    line-height: 1.4;
    letter-spacing: 0.02em;
    color: var(--text-muted);
  }

  .home-stickers__location::before {
    content: '◇ ';
    color: var(--accent-yellow);
    opacity: 0.75;
  }

  @media (max-width: 576px) {
    .home-stickers {
      margin-top: 4rem;
      padding-top: 3rem;
    }

    .home-stickers__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }
  }
</style>
